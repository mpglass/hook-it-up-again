import React from 'react';
import Template from '../components/Template';

const Home = () => {
    return (
        <Template>
            <h1 className="text-center">Click the link to view Users</h1>
        </Template>
    );
};

export default Home; 
